# Pulse
Low-latency, always-on Windows desktop assistant built with a native WPF overlay. It activates via a global keyboard shortcut, captures speech, routes deterministic personal commands instantly, and falls back to an on-device LLM for general queries.
